var read = require('readline-sync');
var flight = {
    desitination: null,
    distance: null,
    fares: null,
};

user.name = read.question("Destination: ");
user.username = read.question("Distance: ");
user.email = read.question("fares: ");

